extends layout

block styles
  link(rel="stylesheet", href="about.css") 

block content
  .container.my-5
    // About Section
    .text-center.mb-5
      h1.display-4 About Us
      p.lead.text-muted We're just three overly caffeinated individuals who thought, "Hey, what if dreams were a product?" And then... we did it.

    .row.mb-5
      .col-md-6
        h2 Our Mission
        p To deliver high-quality, user-focused dreams that won’t explode, vanish unexpectedly, or involve surprise math exams. You're welcome.

      .col-md-6
        h2 Our Values
        ul.list-group.list-group-flush
          li.list-group-item.bg-transparent.border-0.ps-0 ✨ Creativity – Because selling *boring* dreams would be illegal in some countries (probably).
          li.list-group-item.bg-transparent.border-0.ps-0 🧭 Integrity – We only sell ethically sourced dreams. No shortcuts. No dream doping.
          li.list-group-item.bg-transparent.border-0.ps-0 😄 Customer Happiness – If you're not smiling in REM sleep, we're not doing our job.

    .text-center
      a.btn.btn-outline-primary.mt-3(href='/') ⟵ Back to Home (or reality, if you must)

    hr.my-5

    // FAQ Section
    h1.mb-4.text-center Frequently Asked Questions

    #faqAccordion.accordion
      each faq, index in faqs
        .accordion-item
          h2.accordion-header(id=`heading${index}`)
            button.accordion-button.collapsed(type="button", data-bs-toggle="collapse", data-bs-target=`#collapse${index}`, aria-expanded="false", aria-controls=`collapse${index}`)
              = faq.question
          .accordion-collapse.collapse(id=`collapse${index}`, aria-labelledby=`heading${index}`, data-bs-parent="#faqAccordion")
            .accordion-body
              = faq.answer
